// g++ -Wall -std=c++17 -g X.cpp && ./a.out <in>out
#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define _ ios_base::sync_with_stdio(0),cin.tie(0),cout.tie(0);

typedef long long lli;
typedef long double ld;
typedef pair<lli,lli> ii;
typedef vector<lli> vi;

#define f first
#define s second
#define pb push_back
#define sz(c) (lli)c.size()
#define all(c) c.begin(),c.end()
#define fore(i,a,b) for(lli i=(a),_l=(b);i<_l;++i)  // [a -> b)
#define ford(i,a,b) for(lli i=(a),_l=(b);i-->_l;)   // [b <- a)

#ifdef DSTDERR
#define perr(x) cerr<<#x<<" "<<(x)<< endl
#else
#define perr(x) 0
#endif // !DSTDERR

int main() {_
	return 0;
}

// #define NINF LONG_LONG_MIN
// #define PINF LONG_LONG_MAX

// #pragma GCC optimize("Ofast,no-stack-protector,unroll-loops,fast-math")
// #pragma GCC target("avx,avx2,fma")

// #include <ext/pb_ds/assoc_container.hpp>
// #include <ext/pb_ds/trie_policy.hpp>
// using namespace __gnu_pbds;
// using oset = tree<int, null_type, less<int>, rb_tree_tag,
//                   tree_order_statistics_node_update>;
